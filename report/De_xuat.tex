\chapter{TÁI HIỆN KẾT QUẢ VÀ ĐỊNH HƯỚNG CẢI TIẾN}

Chương này chuyển từ phần trình bày nội dung bài báo sang phần thực nghiệm độc lập của nhóm. Mục tiêu trước mắt là tái hiện đúng pipeline của tác giả và tiến tới kết quả gần với các giá trị được công bố. Các kết quả hiện tại được xem là \textbf{baseline tái hiện}, không phải kết quả cải tiến cuối cùng. Chỉ sau khi xác định được một cấu hình tái hiện đủ sát với bài báo, nhóm mới đánh giá các phương án cải tiến và so sánh chúng với baseline cũng như kết quả của tác giả.

\section{Baseline tái hiện hiện tại}

Kết quả tái hiện hiện tại của nhóm được tổng hợp trong Bảng~\ref{tab:reproduction_comparison}. Dataset 1 sử dụng SMOTE và giữ lại 11 thành phần PCA, giải thích khoảng 96.50\% phương sai; Dataset 2 sử dụng Random UnderSampler và cũng giữ 11 thành phần, giải thích khoảng 95.86\% phương sai.

\begin{table}[H]
\centering
\caption{So sánh một số chỉ số được công bố với baseline tái hiện hiện tại}
\label{tab:reproduction_comparison}
\begin{tabular}{lrrr}
\toprule
\textbf{Chỉ số} & \textbf{Bài báo} & \textbf{Tái hiện hiện tại} & \textbf{Chênh lệch}\\
\midrule
Dataset 1 -- CV Accuracy & 0.9532 & 0.7652 & -0.1880\\
Dataset 2 -- CV Accuracy & 0.9903 & 0.9654 & -0.0249\\
Dataset 2 -- MCC & 0.9634 & 0.7322 & -0.2312\\
Dataset 2 -- Cohen's Kappa & 0.9632 & 0.6993 & -0.2639\\
\bottomrule
\end{tabular}
\end{table}

Các chênh lệch trên cho thấy pipeline hiện tại chưa tái lập đầy đủ kết quả của tác giả, đặc biệt trên Dataset 1. Vì vậy, bước tiếp theo của nghiên cứu không phải là tối ưu mô hình để vượt kết quả bài báo ngay lập tức, mà là tái dựng chính xác hơn các bước xử lý dữ liệu, chia tập, cân bằng lớp, PCA, cấu hình XGBoost và giao thức đánh giá của tác giả.

\section{Kết quả trên tập kiểm tra của nhóm}

Trên tập kiểm tra giữ nguyên phân bố mất cân bằng, Dataset 1 đạt accuracy 0.7818, precision 0.1554, recall 0.7800, F1-score 0.2591 và ROC-AUC 0.8118. Dataset 2 đạt accuracy 0.9664, precision 0.5590, recall 0.9944, F1-score 0.7157 và ROC-AUC 0.9976. Đây là các kết quả của pipeline hiện tại và được lưu lại làm mốc để đánh giá tác động của từng thay đổi trong quá trình tái lập.

Các chỉ số khác loại không được so sánh trực tiếp. Chẳng hạn, recall của nhóm chỉ được đối chiếu với recall của tác giả khi xác định được cùng lớp mục tiêu và cùng giao thức đánh giá; không sử dụng accuracy của bài báo để suy luận rằng recall của nhóm tốt hơn hoặc kém hơn.

\section{Thực nghiệm cải tiến cân bằng lớp và ngưỡng dự đoán}

Sau khi hoàn thành baseline tái hiện, nhóm thực hiện một thực nghiệm mở rộng nhằm đánh giá riêng ảnh hưởng của phương pháp cân bằng lớp, PCA và ngưỡng phân loại. Với Dataset 1, ba phương án được so sánh gồm SMOTE, SMOTENC và học có trọng số lớp; mỗi phương án được chạy ở hai cấu hình có và không có PCA. Với Dataset 2, do số lượng bản ghi rất lớn, nhóm so sánh Random UnderSampler với học có trọng số lớp, cũng ở hai cấu hình có và không có PCA.

Trong tất cả các cấu hình, dữ liệu được chia thành tập huấn luyện và tập kiểm tra trước khi ước lượng các tham số tiền xử lý. Imputation, chuẩn hóa, mã hóa, PCA và resampling chỉ được fit trên dữ liệu huấn luyện hoặc bên trong từng fold. Hyperparameter được lựa chọn theo standardized partial ROC-AUC với giới hạn FPR bằng 10\%. Đối với Dataset 2, việc tuning sử dụng tối đa 150.000 dòng từ một phần huấn luyện nội bộ; mô hình cuối cùng được fit lại trên toàn bộ 4.433.808 dòng của tập huấn luyện ngoài khi phương pháp cho phép.

Để phù hợp với mục tiêu sàng lọc ca đột quỵ, nhóm không chỉ sử dụng ngưỡng mặc định 0,5 mà còn lựa chọn ngưỡng nhằm tối đa hóa recall dưới các ràng buộc specificity 85\%, 90\% và 95\%. Các ngưỡng được xác định trên validation hoặc dự đoán out-of-fold của dữ liệu huấn luyện, sau đó được cố định trước khi đánh giá trên tập kiểm tra.

\begin{table}[H]
\centering
\small
\caption{Các cấu hình được lựa chọn trên Dataset 1 tại những mức specificity mục tiêu}
\label{tab:improvement_d1}
\resizebox{\linewidth}{!}{%
\begin{tabular}{p{2.4cm} p{4.2cm} r r r r r r}
\toprule
\textbf{Spec. mục tiêu} & \textbf{Cấu hình} & \textbf{Ngưỡng} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{Specificity} & \textbf{F1}\\
\midrule
85\% & Trọng số lớp + PCA & 0,6704 & 84,34\% & 19,44\% & 70,00\% & 85,08\% & 0,3043\\
90\% & Trọng số lớp, không PCA & 0,1305 & 87,57\% & 18,70\% & 46,00\% & 89,71\% & 0,2659\\
95\% & Trọng số lớp, không PCA & 0,1902 & 91,88\% & 23,81\% & 30,00\% & 95,06\% & 0,2655\\
\bottomrule
\end{tabular}}
\end{table}

Trên Dataset 1, cấu hình được chọn tại specificity mục tiêu 85\% phát hiện 35 trên 50 ca dương tính, đồng thời giữ specificity ở mức 85,08\%. Khi sử dụng ngưỡng mặc định 0,5, cấu hình SMOTENC không PCA cho F1 cao nhất trong nhóm cấu hình khảo sát, với accuracy 92,95\%, precision 29,63\%, recall 32,00\% và F1 bằng 0,3077. Kết quả cho thấy không có một cấu hình duy nhất tối ưu đồng thời mọi chỉ số; lựa chọn mô hình cần gắn với chi phí của bỏ sót ca bệnh và báo động giả.

\begin{table}[H]
\centering
\scriptsize
\caption{Kết quả Dataset 2 trên tập kiểm tra giữ nguyên phân bố tự nhiên}
\label{tab:improvement_d2}
\resizebox{\linewidth}{!}{%
\begin{tabular}{p{3.6cm} p{3.4cm} r r r r r r}
\toprule
\textbf{Cấu hình} & \textbf{Chính sách} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{Specificity} & \textbf{F1} & \textbf{ROC-AUC}\\
\midrule
Undersampling + PCA & Ngưỡng 0,5 & 98,47\% & 74,01\% & 98,86\% & 98,46\% & 0,8465 & 0,9987\\
Undersampling + PCA & Specificity 85\% & 86,11\% & 23,46\% & 100,00\% & 85,50\% & 0,3801 & 0,9987\\
Undersampling + PCA & Specificity 90\% & 91,22\% & 32,65\% & 99,98\% & 90,83\% & 0,4922 & 0,9987\\
Undersampling + PCA & Specificity 95\% & 95,18\% & 46,90\% & 99,84\% & 94,97\% & 0,6383 & 0,9987\\
\bottomrule
\end{tabular}}
\end{table}

So với kết quả balancing + PCA đã tuning trước đó trên cùng tập kiểm tra gốc, cấu hình undersampling + PCA tại ngưỡng 0,5 làm accuracy tăng từ 96,64\% lên 98,47\%, precision từ 55,90\% lên 74,01\%, F1 từ 0,7157 lên 0,8465 và ROC-AUC từ 0,9976 lên 0,9987. Recall giảm nhẹ từ 99,44\% xuống 98,86\%, nhưng số báo động giả giảm đáng kể. Đây là cải thiện theo hướng cân bằng tốt hơn giữa phát hiện ca bệnh và kiểm soát báo động giả, không phải cải thiện đồng thời mọi chỉ số.

Đáng chú ý, recall 100\% của chính sách specificity 85\% trên Dataset 2 không đủ để kết luận mô hình bị overfit hoặc đạt độ chính xác tuyệt đối. Ngưỡng 0,0462 khá thấp nên mô hình dự đoán dương tính cho nhiều trường hợp; kết quả tương ứng có 153.928 báo động giả và precision chỉ 23,46\%. Hơn nữa, recall trên validation là 99,984\% và trên test là 100\%, trong khi specificity lần lượt là 85,66\% và 85,50\%, cho thấy hai tập có hành vi gần nhau. Dù vậy, test vẫn là split đã sử dụng trong các thực nghiệm trước và Dataset 2 có nguồn synthetic; vì vậy cần dữ liệu ngoài và đánh giá lặp hoặc nested cross-validation trước khi suy rộng kết luận.

\section{Đối chiếu hiệu năng PCA và OpenMP}

Thực nghiệm benchmark hiện tại cho thấy hiệu quả song song hóa phụ thuộc vào kích thước ma trận. Với $N=100$ và $N=200$, speedup lần lượt đạt 0.604 và 0.923. Khi kích thước tăng, speedup đạt 1.084 tại $N=300$, 1.251 tại $N=400$ và 1.539 tại $N=500$.

Kết quả này xác nhận xu hướng OpenMP trở nên hữu ích hơn khi khối lượng tính toán tăng, nhưng chưa tái lập mức tăng tốc khoảng ba lần được báo cáo. Do đó, phần OpenMP cũng được xem là một bài toán tái lập độc lập: cần kiểm tra thuật toán Jacobi, số luồng, compiler flags, kích thước ma trận, số lần lặp và phương pháp đo thời gian trước khi đánh giá cải tiến.

\section{Nguyên tắc tái lập và đánh giá}

Quá trình tái lập và mở rộng được thực hiện theo nguyên tắc chỉ thay đổi có kiểm soát từng thành phần của pipeline. Nhóm khóa các giá trị mục tiêu từ bài báo gồm Accuracy, Precision, Recall, F1-score, MCC, Cohen's Kappa, confusion matrix và thời gian chạy khi tác giả có công bố. Các thành phần được kiểm tra gồm:

\begin{enumerate}
  \item \textbf{Dữ liệu đầu vào:} số bản ghi, số đặc trưng, tỷ lệ lớp, giá trị thiếu, bản ghi trùng và cách xử lý ngoại lệ.
  \item \textbf{Tiền xử lý và chia dữ liệu:} encoding, scaling, tỷ lệ train/test, stratification, random seed và vị trí của từng bước trước hoặc sau khi chia tập.
  \item \textbf{Cân bằng lớp:} SMOTE hoặc undersampling được áp dụng trên phần dữ liệu nào và ở thời điểm nào trong pipeline.
  \item \textbf{PCA:} chuẩn hóa đầu vào, cách tính PCA/Jacobi, tiêu chuẩn chọn số thành phần và việc fit PCA chỉ trên train hay trên phạm vi dữ liệu khác.
  \item \textbf{XGBoost:} toàn bộ hyperparameter, objective, random seed, threshold và cơ chế cross-validation.
  \item \textbf{Đánh giá:} xác nhận cùng định nghĩa lớp dương, cùng cách average metric và cùng tập dữ liệu khi so sánh với số liệu tác giả.
\end{enumerate}

Mỗi thay đổi sẽ được lưu thành một experiment riêng cùng cấu hình và bộ chỉ số đầu ra. Cách làm này cho phép xác định thành phần nào giải thích phần chênh lệch giữa baseline hiện tại và kết quả công bố, thay vì thay đổi đồng thời nhiều yếu tố và không thể truy nguyên nguyên nhân.

\section{Định hướng cải tiến tiếp theo}

Các thực nghiệm tiếp theo nên tập trung vào đánh giá bằng PR-AUC và các chỉ số phù hợp với mất cân bằng lớp; kiểm định ngưỡng trên nhiều lần chia dữ liệu; so sánh nhiều mức phương sai PCA và pipeline không PCA; thử cost-sensitive XGBoost, \texttt{scale\_pos\_weight}, SMOTE-Tomek hoặc các phương pháp cân bằng khác; và thực hiện ablation study để tách riêng đóng góp của PCA, cân bằng lớp và XGBoost. Đặc biệt, cần xây dựng một tập kiểm tra độc lập với split hiện tại để đánh giá khả năng tổng quát hóa trước khi cân nhắc ứng dụng thực tế.

Kết quả cuối cùng sẽ được trình bày theo ba cột: \textbf{kết quả tác giả -- kết quả tái hiện -- kết quả cải tiến}. Một phương án chỉ được kết luận là tốt hơn tác giả khi cùng metric, cùng lớp mục tiêu và giao thức đánh giá đủ tương đồng; đồng thời phải chỉ rõ lợi ích đạt được ở Accuracy, Recall, F1-score, MCC, khả năng diễn giải hoặc thời gian tính toán.
